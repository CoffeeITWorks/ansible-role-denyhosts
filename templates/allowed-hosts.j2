# {{ ansible_managed }}
# denyhosts_allowed_hosts: {{ denyhosts_allowed_hosts|length }}
{% if denyhosts_allowed_hosts|length > 0 %}

{%   for allowed_host in denyhosts_allowed_hosts %}
{{ allowed_host }}
{%   endfor %}
{% endif %}
